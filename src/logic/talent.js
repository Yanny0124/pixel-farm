// src/logic/talent.js
import { State } from '../core/state.js';

export function getSkillCd(id) { /* 搬 systems.js */ }
export function getSkillCost(id) { /* 搬 systems.js */ }
export function upgradeSkill(id) { /* 搬 systems.js */ }
export function useSkill(id) { 
  // 搬 engine.js 的 useSkill，但把具体效果委托给 Farm / Weather
}